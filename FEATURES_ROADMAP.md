# ICU杯 機能拡張ロードマップ

最終更新: 2026-06-14

「ICU杯」を **複数のゴルフコンペを管理できる汎用ポータル** に育てるための計画。

## 確定した方針（2026-06-14 決定）
- **複数大会（グループ）対応から着手**（全機能の土台）
- 賞の自動計算は **ホール単位スコア** を導入して対応
- アクセスは **グループ別**（グループごとにアクセスコード）

---

## 全体像（依存関係）

```
[グループ(複数大会)]  ← 土台。これを最初に作る
   ├─ プレーヤー（グループごと）
   ├─ コース（共有）        ← 「コース簡単追加」で DB化
   ├─ 大会(events)
   │     ├─ ホール単位スコア ← 「賞の自動計算」で追加
   │     ├─ 写真            ← 「写真保存」
   │     └─ 賞（自動計算）
   └─ 幹事ツール（日程調整・乗り合わせ） ← 大会の前段
```

---

## フェーズ1: 複数大会（グループ）対応 ★今ここ

### 目的
ICU杯だけでなく「家族コンペ」など複数の独立したコンペを1アプリで管理。

### データモデル
```sql
CREATE TABLE groups (
  id TEXT PRIMARY KEY,
  name TEXT NOT NULL,           -- 例: ICU杯 / ○○家コンペ
  access_code TEXT NOT NULL,    -- グループごとのアクセスコード
  created_at TIMESTAMPTZ DEFAULT now()
);
ALTER TABLE players ADD COLUMN group_id TEXT;  -- プレーヤーはグループ所属
ALTER TABLE events  ADD COLUMN group_id TEXT;  -- 大会もグループ所属
-- courses はグループ横断で共有（ゴルフ場は共通資産）
```

### アクセス設計（グループ別）
- ログイン画面でグループの **アクセスコード** を入力 → そのグループを解錠
- Cookie に「解錠済みグループID一覧」＋「現在のグループ」を保持
- 複数グループのコードを入れれば、ヘッダーの **グループ切替** で行き来できる
- 既存 `SITE_PASSWORD` は移行時に「ICU杯」グループの access_code として引き継ぐ
  （＝今のメンバーは今までのパスワードでそのまま入れる）

### 移行（既存データを壊さない）
1. `groups` 作成 + `group_id` 列追加
2. デフォルト「ICU杯」グループを作成（access_code = 現 SITE_PASSWORD）
3. 既存の players / events を全て ICU杯グループに割当

### 作業
- [ ] マイグレーション API
- [ ] データ層をグループでスコープ（getPlayers/getEvents 等）
- [ ] セッションヘルパー（現在グループの取得）
- [ ] ログインをアクセスコード方式に変更
- [ ] ヘッダーにグループ切替
- [ ] 管理画面でグループ作成・コード発行
- [ ] 各ページを現在グループ基準に

---

## フェーズ2: コース簡単追加

- `courses.json`（静的）→ `courses` テーブルへ移行
- `/admin/courses` でコース名・CR・スロープを入力して追加・編集
- 既存 courses.json を初期データとして投入

---

## フェーズ3: ホール単位スコア & 賞の自動計算

### データ
```sql
CREATE TABLE hole_scores (
  event_id TEXT,
  player_id TEXT,
  hole INT,        -- 1..18
  par INT,         -- そのホールのパー（コース側に持たせてもよい）
  strokes INT,
  PRIMARY KEY (event_id, player_id, hole)
);
```
- コースに各ホールの par / HDCP を持たせる（コース定義拡張）
- OUT/IN/合計はホール合計から自動算出

### 自動計算する賞（例）
| 賞 | 定義 |
|----|------|
| 優勝(Net) | ネット最少 |
| グロス賞 | グロス最少 |
| BB賞(ブービー) | 下から2番目 |
| 大波賞 | 1ラウンド中の最大スコアのホール（最も叩いた） |
| 小波賞 | 全ホール中の最少スコア |
| パーディ賞 | パー以下のホール数最多 |
| ニアピン/ドラコン | 手入力（自動化不可） |

---

## フェーズ4: 写真保存

- ストレージ: **Vercel Blob**（@vercel/blob）
- `event_photos` テーブル（event_id, blob_url, uploaded_by, created_at）
- 既存の `events/[id]/photos` ページにアップロード&ギャラリー実装
- iOSアプリではネイティブのカメラ/写真連携も将来検討

---

## フェーズ5: 幹事ツール（日程調整・乗り合わせ）

### 日程調整（調整さん風）
- `event_polls`（候補日）＋ `poll_votes`（参加者 × 候補日 × ○△×）
- 最も票が集まった日を可視化

### 車の乗り合わせ
- `carpools`（運転者・車・定員・出発地・時刻）＋ `carpool_seats`（同乗者）
- 空席状況を表示、参加者が乗車登録

---

## 想定スケジュール感
1. フェーズ1（グループ）★最優先
2. フェーズ2（コース追加）小さく即効
3. フェーズ3（ホール単位＋賞）
4. フェーズ4（写真）
5. フェーズ5（幹事ツール）

> 各フェーズは独立してデプロイ可能な単位で進める。
